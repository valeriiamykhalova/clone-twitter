/**
 *
 * User
 *
 */

type User {
  // The parent key of the current location is equal to "id" property
  validate() { this.id == key() }

  id: String,
  email: String,
  firstName: String | Null,
  lastName: String | Null,
  username: String | Null,
  image: File | Null,
}